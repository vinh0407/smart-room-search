# Dev mặc định trong api.ts: /api (Vite proxy → http://localhost:4000 hoặc 8787)
# Production (Cloudflare Workers): URL BE thật — thay <your-subdomain>
VITE_API_URL=https://smart-room-api.<your-subdomain>.workers.dev/api