
  # Smart Room Search Website

  Frontend cho Smart Room Search — tìm phòng trọ kết nối backend Cloudflare Worker (`smart-room-api`) và TiDB Cloud.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  